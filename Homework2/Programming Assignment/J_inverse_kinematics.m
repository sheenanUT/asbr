% find the inverse kinematics using an iterative numerical algorithm
function thetalistd = J_inverse_kinematics(home_config, thetalist0, Tsd)
    i = 0;
    thetalist = thetalist0;     % initial guess

    FK_body(theta_list)
    thetalistd = 1;
end